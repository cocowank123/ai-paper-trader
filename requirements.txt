# Dependencies for the Quant Regime Engine (QRE) strategy.
# Install with:   pip install -r requirements.txt

yfinance>=0.2     # Free USD/JPY (and other) market data, no account needed
pandas>=2.0       # Indicator math (Z-Score, EMA, ATR)
